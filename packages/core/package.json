{
  "name": "@shiperist-catppuccin-ui/core",
  "version": "0.0.1",
  "main": "src/index.ts",
  "sideEffects": false,
  "license": "MIT",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --external react",
    "dev": "tsup src/index.ts --format esm,cjs --watch --dts --external react",
    "lint": "eslint \"src/**/*.ts*\"",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist"
  },
  "devDependencies": {
    "@catppuccin/tailwindcss": "^0.1.1",
    "tailwindcss": "^3.2.4",
    "@types/react": "^18.0.9",
    "@types/react-dom": "^18.0.4",
    "postcss": "^8.4.20",
    "autoprefixer": "^10.4.14",
    "eslint": "^8.15.0",
    "react": "^18.1.0",
    "tsup": "^5.10.1",
    "typescript": "^4.5.3"
  }
}
